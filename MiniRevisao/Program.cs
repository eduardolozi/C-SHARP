using Calculo;
using Files;
using Tela;

namespace MiniRevisao
{
    class Program{
        static void Main(string[] args)
        {
            // Fui criando pastas(namespaces) com classes dentro delas, essas classes possuem funçoes proprias
            //CLASSE.FUNÇAO() (FUNCIONA SE EU FIZER USING NAMESPACE DA CLASSE)
            Menu.Criar();
        }
    }
}
